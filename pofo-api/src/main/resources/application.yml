spring.profiles.active: local

spring:
  config:
    import:
      - application-db.yml
  web:
    resources:
      add-mappings: false

logging:
  level:
    org.pofo.api: DEBUG
    org.springframework.security: DEBUG
